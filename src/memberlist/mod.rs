//! Cluster membership via a gossip protocol (feature `memberlist`).
//!
//! This module will track which nodes are alive, detect failures, and
//! disseminate membership changes across the cluster. It is a placeholder for
//! now — the gossip transport and failure detector are not yet implemented.
